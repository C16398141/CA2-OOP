class Droids extends Body {
 
  float cx;
  float cy;
  float r,t;
  
  Droids(Snakehead head){
    cx=head.x;
    cy=head.y;
    diameter=5;
    r=head.diameter*2;
  }
  
  void draw(Snakehead head)
  {
    cx=head.x;
    cy=head.y;
    t = millis()/400.0;
    x = (int)(cx+r*cos(t));
    y = (int)(cy+r*sin(t));
    
    ellipse(x,y,diameter,diameter);

  }
}
